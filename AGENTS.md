# everyDock 작업 지침

- Python이 필요한 작업은 uv로 환경과 의존성을 관리한다.
- 코드와 docs를 같은 변경 단위에서 유지한다. 동작·구조·성능·권한·배포를 변경하면 관련 PRD/SRS/TC와 README를 함께 수정한다.
- docs의 요구사항을 바꾸면 구현과 테스트를 대조하고 필요한 코드 변경을 함께 반영한다. 실제 코드와 다른 요구사항을 구현 완료로 표시하지 않는다.
- 요구사항 ID를 유지하고 추가 테스트에 연결한다. 검증 결과 갱신에서는 실제로 수행한 범위만 PASS로 기록한다.
- 공개한 태그나 릴리스 자산은 덮어쓰지 않는다. 사용자 설정, 권한 DB, 인증 키, 창 이미지와 개인 파일을 커밋하지 않는다.
- macOS 권한 거부, 실행 파일/등록 불일치, API 오류, 지원하지 않는 창, 빈 창 목록을 구분한다. 모든 실패를 권한 문제로 안내하지 않는다.
- UI 경로에서 동기식 앱 전체 조회·파일 읽기를 반복하지 않는다. 애니메이션은 화면 동기화와 캐시를 사용하고 실제 측정 근거를 남긴다.
