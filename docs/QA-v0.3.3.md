# v0.3.3 검증 기록

기준일 2026-09-09. 창 1개 미리보기 맞춤 크기, 바탕화면 파일 스택, Apps 검색, 확대 창의 Dock 영역 보정.

- `swift test --arch arm64` **31개 PASS**. 1/2/여러 창 레이아웃, 아래/양옆·음수 좌표 보정, 전체 화면·최소화·일반 크기 제외, Apps 식별·번들 내부 helper 제외를 추가했다.
- **TC-M43 PARTIAL PASS**: 설치 앱 목록 팝업을 열고 이름 검색으로 계산기를 찾아 실제 실행했다. 별도 AX 창 활성화·최소화 경로를 타지 않는다. 모든 앱 유형 실행 행렬은 미실행이다.
- TC-M41/M42/M44/M45 실기기 결과는 검증 후 아래에 기록한다. 자동 좌표 테스트는 실제 AX 크기 변경·타이틀 더블클릭·전체 화면 전환 검증을 대신하지 않는다.
- 이전 릴리스 [QA-v0.3.2](QA-v0.3.2.md)의 사용자 확인과 성능은 이 버전 전체 PASS로 재사용하지 않는다.

검증은 파일을 만들지 않는 WindowFixture와 기존 앱의 비파괴 조작으로 수행한다. 실제 창 이미지·개인 파일명·원본 진단 로그를 저장소에 올리지 않는다.
